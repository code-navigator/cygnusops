v-expansion-panel
  v-expansion-panel-header
    strong Search by Category
  v-expansion-panel-content
    v-radio-group.flex-center(
      column
      v-model="radios"
    )
      v-radio(
        v-for="(category, i) in categories"
        :key="i"
        :label="category.name"
        :value="category.slug"
      )
    v-divider
    p.subtitle-1.ml-5.pt-5 Search Results
    p.ml-5 {{ searchResults }}